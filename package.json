{
  "name": "qouria-backend",
  "version": "1.0.0",
  "description": "a courier service that helps users deliver parcels to different destinations",
  "main": "app.js",
  "scripts": {
    "babel-node": "babel-node --presets=env",
    "start": "npm run build && node build/bin/index.js",
    "build": "babel server --out-dir build",
    "test": "npm run build && mocha --timeout 10000 build/test/"
  },
  "author": "Joseph",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.18.3",
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "connect-flash": "^0.1.1",
    "cors": "^2.8.5",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "jsonwebtoken": "8.4.0",
    "mocha": "^5.2.0",
    "mongoose": "^5.4.1",
    "morgan": "^1.9.1",
    "nyc": "^15.0.0",
    "supertest": "^4.0.2",
    "swagger-ui-express": "^4.1.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
